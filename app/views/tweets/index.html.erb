<div class="container" >
  <h1 class="text-center font-weight-bold mb-5">みんなの絵にっき</h1>
  <% @tweets.each do |tweet| %>
    <%= render(tweet) %>
  <% end %>
  <div class="text-center"><%= paginate(@tweets) %></div>
</div>